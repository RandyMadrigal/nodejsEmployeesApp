<main>

  <div
    class="row row-cols-1 row-cols-sm-1 row-cols-md-2 g-3 justify-content-center py-5"
  >
    <div class="col">
      <div class="card shadow-sm text-center">

        <div class="card-body">
          <p class="card-title fw-bold fs-4">
            ¿Desea cancelar las vacaciones de {{item.Nombre}} {{item.Apellido}}?</p>

          <p class="card-text fw-bold fs-5">
            Desde: {{item.StartVacaciones}} /  Hasta: {{item.EndVacaciones}}
          </p>

          <div class="d-flex justify-content-center align-items-center">
                <form action="/cancel" method="post">

                    <button
                    type="submit"
                    class="btn btn-lg btn-danger"
                    >Si
                    </button>

                    <a
                    type="button"
                    href="/vacation-manager"
                    class="btn btn-lg btn-success"
                    >No
                    </a>

                    <input type="hidden" name="Id" value="{{item.Id}}" />
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>